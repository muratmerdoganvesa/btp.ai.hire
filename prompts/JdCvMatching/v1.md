# JdCvMatching v1

Score each criterion only when the masked CV contains a quotable span.
Return source, quote, and character offsets with every numeric score.
If evidence is missing, score must be null and evidenceStatus Insufficient.
